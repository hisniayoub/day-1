#include <stdio.h>

int main() {
    int a , b, r;
    
    printf("enter la valeur a :");
    scanf("%d",&a);
    printf("enter la valeur b :");
    scanf("%d",&b);
    
    if(a==b){
 r=a+a+a;
     printf("la triple de la somme de deux cet valeurs est :%d",r);
    }
    
    

    return 0;
}